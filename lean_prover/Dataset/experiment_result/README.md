# Evaluation results

Benchmark and rollout commands write generated artifacts here. Payload files
are excluded from Git.

Each run produces a detailed JSONL and a report JSON. Canonical prefixes are
`benchmark`, `sft_rollout`, and `grpo_rollout`; report files end in
`_report.json`.
